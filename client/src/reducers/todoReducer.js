const initialState = {
    todos: []
};
export default function(state = initialState, action) {
    var newState;
    switch (action.type) {
        default:
            newState = {};
    }
    return newState;
}
